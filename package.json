{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "dev": "tsx watch --env-file=.env src/server.ts",
    "start": "node dist/server.js",
    "pre-build": "npm install && prisma migrate deploy && prisma generate && npm run build",
    "prisma:generate": "prisma generate",
    "build": "tsup src/server.ts --format esm --out-dir dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@fastify/cors": "^11.3.0",
    "@fastify/jwt": "^10.2.2",
    "@fastify/multipart": "^10.1.0",
    "@fastify/static": "^10.1.3",
    "@prisma/adapter-pg": "^7.9.1",
    "@prisma/client": "^7.9.1",
    "bcrypt": "^6.0.0",
    "fastify": "^5.11.2",
    "google-auth-library": "^11.0.2",
    "nodemailer": "^9.0.5",
    "prisma": "^7.9.1"
  },
  "devDependencies": {
    "tsup": "^8.5.1",
    "tsx": "^4.23.9",
    "typescript": "^5.9.2"
  }
}
